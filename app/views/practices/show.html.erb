<main class="main">
  <div class="inner">
    <div class="practice__wrapper">
      <p class="practice__hedding">
        <%= @practice.attempt.name%></font>
      </p>
      <%= link_to "投稿者　 #{@practice.user.nickname}", root_path, class: :practice__user %>
      <% if @practice.user_id == current_user.id %>
        <div class="practice__manage">
          <%= link_to "編集する", root_path, class: :practice__btn %>
          <%= link_to "削除する", root_path, class: :practice__btn %>
        </div>
      <% end %>  
      <div class="practice__image">
        <%= video_tag url_for(@practice.video), class: :card__img, controls: true %>
      </div>
      <div class="practice__body">
        <div class="practice__detail">
          <p class="detail__title">～見てほしいポイント～</p>
          <p class="detail__message">
            <%= @practice.point %>
          </p>
        </div>
      </div>
      
      <div class="practice__comments">
        <%# ログインしているユーザーには以下のコメント投稿フォームを表示する %>
          <%# <%= form_with model: モデル名,local: true do |f|%>
            <div class="field">
              <%# <%= f.label :hoge, "コメント" %><br />
              <%# <%= f.text_field :hoge, id:"comment_content" %>
            </div>
            <div class="actions">
              <%# <%= f.submit "送信する", class: :form__btn  %>
            </div>
          <%# <% end %>
        <%# // ログインしているユーザーには上記を表示する %>
        <ul class="comments_lists">
          <%# 投稿に紐づくコメントを一覧する処理を記述する %>
            <li class="comments_list">
              <%# <%= " コメントのテキスト "%>
              <%# <%= link_to "（ ユーザー名 ）", root_path, class: :comment_user %>
            </li>
          <%# // 投稿に紐づくコメントを一覧する処理を記述する %>
        </ul>
      </div>
    </div>
  </div>
</main>
